apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "reciping-user-service.fullname" . }}-config
  labels:
    app: {{ include "reciping-user-service.name" . }}
data:
  application-prod.yaml: |-
{{ .Files.Get "files/application-prod.yaml" | indent 4 }}