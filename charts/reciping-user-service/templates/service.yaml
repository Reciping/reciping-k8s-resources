apiVersion: v1
kind: Service
metadata:
  name: reciping-user-service
  namespace: reciping
  labels:
    app: {{ include "reciping-user-service.name" . }}
spec:
  selector:
    app: {{ include "reciping-user-service.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
